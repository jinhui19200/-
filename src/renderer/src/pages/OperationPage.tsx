export function OperationPage(): React.JSX.Element {
  return (
    <section className="card">
      <h2>操作</h2>
      <p className="empty">
        出库、入库两个竖块将在下一阶段实现。每块自上而下填写时间 / 名称 / 数量 / 单位，
        各带一个提交按钮。
      </p>
    </section>
  )
}

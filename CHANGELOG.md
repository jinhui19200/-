# 更改记录

> 每次提交前更新本文件，**最新记录在最上面**。
> 格式：日期（精确到日）+ 修改功能简述。

## 2026-09-23

### 物品改名（双击 / 右键，撞名合并）+ 仓库页「单位」「警戒值」列宽收窄（v1.4.0）

**改名：仓库页与记录页都能改，三页同步**

- 名称格**双击**进入行内编辑，**右键**弹出菜单里也有「修改名称」。
  回车 / 失焦提交，Esc 取消。鼠标移上去浮一条虚线下边框，暗示这里能改
- 改完之后**仓库 / 记录 / 报表三页一起变**。这条要特别说明：
  `StockRecord.name` 原本刻意存快照（「历史记录是铁证」），改名是**唯一的例外** ——
  名字只是物品的标签，不是当时的业务事实。改完不同步的话，记录页会一直显示旧名字，
  用户会以为那是另一个物品。**但记录的 `unit` 快照不动**：一条「12 包」的历史记录，
  哪怕物品后来并进了按「个」计数的物品，它也确实是 12 包，改掉就是篡改历史
- 在**记录页**改名 = 改**整个物品**（它名下所有记录一起更新），不是只改那一条。
  记录里的 name 只是物品的标签快照，单独改一条会让它和所属物品对不上

**撞名即合并**

- 新名字已被别的物品占用时，把本物品的记录搬到目标物品名下、数量累加、删掉本物品
- 合并前**必弹确认框**，写清楚「合并后共多少、要搬走几条记录」，并明说**不可撤销**
  （源物品会被删掉）。取消必须真的什么都不做 —— 有断言盯着
- **两边单位不一致时，确认框里追加「合并后按哪个单位算」**：两个单位各给一个按钮，
  外加一个输入框可以填新单位。默认选中的是**保留下来的那个物品**的单位。
  这是唯一会让库存数量失去物理意义的路径（2 个 + 12 包 = 14 个），不能静默走掉。
  单位一致时**不出现**这个区域，不让用户白选一次
- 选单位必须在**写盘之前**定下来，否则得先写一次（已经合并了）再补一次改单位，
  两次写之间进程挂掉就留下脏数据。所以界面用自己手里的快照先判断，
  数据层返回的 `unitConflict` 用来**事后核对** ——
  两者不一致说明界面快照过期了，会再弹一次提示让用户核对库存

**仓库页列宽**

- 「单位」列 87px → **56px**，「警戒值」列 171px → **100px**，
  两列之间的实际空白 **135px → 44px**（用户圈出来的就是这一段）
- 根因不是列宽写错了，是**默认的自动列宽会把 100% 宽里的富余空间按内容比例摊给每一列**：
  「单位」的表头文字只有 25px 却被撑到 87px，「警戒值」被撑到 171px。
  改成 `table-layout: fixed` 定宽列，富余宽度只留给**没写宽度的「名称」列**
- 各列宽度写在 JSX 的 `<colgroup>` 里，和表头挨着，改列时不容易漏。
  刻意**只给仓库页加这个类**：记录页列数不同、也没有需要压窄的列，跟着改会让表头换行

**验证**

| 层 | 项数 | 变化 |
| --- | --- | --- |
| 类型检查 | 0 错误 | — |
| verify:store | 131 | +31 |
| verify:electron | 81 | +19 |
| verify:ui | 269 | +68 |
| verify:app | 41 | — |
| verify:packaged | 40 | — |

**补掉一条没走到的路径**：界面层的合并用例一开始只从**仓库页**发起过，
但需求里「记录页也能改名」是明确写了的 —— 从记录页撞名合并是另一条代码路径：
源物品的记录正被列表遍历着，合并会把它们整体改嫁到目标物品名下、再把源物品从
`items` 里删掉，**列表与物品表同时变化**。补了 9 项断言覆盖（弹框、单位区不出现、
记录页里旧名一条不剩、仓库页物品数少一、报表页卡片同步）。

**又补掉一条同类路径**：出入库也有**两个入口** ——「操作」页的常驻表单（第 6 节测的），
以及仓库页每行的「入库 / 出库」按钮弹出的对话框。后者此前**一层都没跑过**：
`verify:electron` 直接调 IPC、`verify:app` 走操作页表单，都不经过它。
两者共用 `TransactionForm`，但挂载方式与收尾不同（条件渲染、预填物品名、
单位锁定、提交后自己关窗、`onCancel` 只在弹窗里传）。补了 16 项断言。

顺带钉住一个隐患：对话框的 `.modal` 类名与改名合并框**共用**，
光数 `.modal` 分不清是哪一个 —— 新断言一律按**标题语义**（`入库 — <物品名>`）定位。

同一节里按「入口 × 分支」的矩阵又补了弹窗的**出库**分支（上面只提交过入库）：
出库有自己的负库存告警路径，且表单的经手人/领取人标签跟着 `type` 变。

**抓到一个测试框架自身的缺陷（最有价值的一条）**：第 9 节（导出 Excel）
为了绕开阻塞式对话框，写了 `window.alert = () => {}`，**用完没有还原**。
它是**全局**的，于是后面 9b / 9c 里所有依赖 alert 的断言全部**静默失效** ——
代码确实调了 `alert`、文案也完全正确，只是 dialog 事件永远不会到 Node 侧，
断言只能看到空数组，看起来就像「这个功能没弹警告」。

定位手段是用探针把 `window.alert` 拦下来看调用记录 —— 证明
**「dialog 没到」和「alert 没调」是两回事**，先分清再动断言。
修法是存下原函数、用完还原，并加两条守卫断言（还原后必须指向原函数、
且不再是那个桩）；同时把 `window.api.exportXlsx` 也一并还原。

**顺带把负库存告警的文案钉死**：这句话在真实数据层（`transactions.ts`）与
界面自检的内存替身（`preview/mock.ts`）里**各写了一份**，而此前两层都只查
`typeof warning === 'string'` —— 替身改了、真实实现没改，界面层照样绿，
用户看到的却是另一句话。现在 `verify-store` 与 `verify-ui` 各钉同一串字面量，
改一处必有一层变红。

**补掉一个验证缺口**：`db:renameItem` 这条通道一开始**没有任何一层真跑过它** ——
`verify:ui` 用的是预览版的内存 mock，通道名写错、主进程忘了注册 handler、
preload 没暴露，那边**全都是绿的**。所以补了一节 `verify:electron`：
另开一个数据目录（刻意不共用，因为改名会写盘、写盘会轮转 `.bak`，
会毁掉第 11 节「备份 = 撤销前的状态」这个前提），真经一次
contextBridge + IPC + 磁盘往返。

**反向验证（必做，证明断言不是空跑）**：

- 去掉「改名时同步记录名称快照」→ 记录页改名相关断言变红 ✓
- 去掉「合并时累加数量」→ 「数量按「卷」累加」变红 ✓
- 关掉单位冲突检测（数据层）→ 「带回 unitConflict」变红 ✓
- 关掉单位冲突检测（界面）→ 4 条单位选择断言全变红 ✓
- 去掉改名后的 `broadcastChanged()` → **只有**「界面同步显示新名字」变红，其余 80 项照绿 ✓
- 去掉「从记录页合并时同步名称快照」→ 「记录页里被并掉的名称一条不剩」变红（报「9 条残留」）✓
- 去掉弹窗的 `initialName` → 「名称已预填」与「单位已带出并锁定」两条变红 ✓
- 去掉弹窗提交后的 `closeModal()` → **只有**「提交成功后弹窗自己关掉」变红 ✓
- 去掉第 9 节末尾的 `window.alert` 还原 → 守卫断言 + 9c 的 2 条告警断言共 4 条变红，
  且守卫明细直接指出「原函数存下来了但没还原」（`hadOrigAlert:true, okAlert:false`）✓
- 右键菜单**真的抓到过一个 bug**，见下

**这一节自己也踩了一次坑（值得记）**：第一版断言取「记录页第一行」来认新记录，
结果**红了** —— 第一行是第 6 节创建的那条。原因是两条记录的时间**显示到同一分钟**，
谁排第一取决于排序对并列的处理。这正是「别用行位置定位」的典型反例：
改成按**内容指纹**（名称|类型|数量|单位）计数、比提交前后多了几条，与行序无关。

**同类的第二个坑**：新守卫断言第一版写成「还原**之前**就比较 `window.alert === __origAlert`」，
必然为假（那时桩还装着），而且漏了「原函数是否真的存下来」这个前提 ——
一旦 `__origAlert` 是 `undefined`，`undefined === undefined` 也会让判据为真。
改成「先还原、再比较，且要求原函数存在且确实不再是那个桩」。

**发布**

推 `v1.4.0` tag 触发 CI（run #11），三个 job 全绿：`verify` → `build-mac` + `build-win`。

| 产物 | 体积 |
| --- | --- |
| `warehouse-manager-1.4.0-arm64.dmg`（macOS arm64） | 121.3 MB |
| `warehouse-manager-1.4.0-setup.exe`（Windows x64） | 106.5 MB |

本机同时打了一份做验证（`dist/`），`verify:packaged` 从挂载的 DMG 真启动 **40/40 通过**：
preload 链路通、渲染资源确实来自 `app.asar`、报表页全部功能正常。

- 签名：ad-hoc（`identity: '-'`）。`codesign -dv` 显示 `Signature=adhoc`、
  `Identifier=com.jinhui.warehouse-manager`，`codesign --verify --deep --strict` 退出码 0
- `Info.plist` 里 `CFBundleShortVersionString = 1.4.0`（不是只看文件名）
- 本地 DMG 134MB vs CI 121.3MB：差的只是**压缩级别**，不是内容 ——
  绕开 dmgbuild 用 `hdiutil` 出图只能用默认压缩级别。功能无差别
- 交付时需说明：macOS 未公证，首次打开要「右键 → 打开」；
  Windows 未签名，首次运行要过 SmartScreen（「更多信息 → 仍要运行」）

**⚠️ 打包 ≠ 发布 —— 这一步一开始漏了（2026-09-23 补上）**

打包脚本里带了 `--publish never`（当初是为了防止 tag 构建时 electron-builder
隐式去发 Release、没 `GH_TOKEN` 就报错把已经打好的包一起带挂）。
副作用是：**包打好了，但仓库里一个 Release 都没有** —— v1.2.0 / v1.3.0 也一样，
产物只躺在 GitHub Actions 的 artifacts 里（要登录 GitHub 才能下，而且会过期），
对外**没有可下载的页面**。

→ 用 `gh release upload` 补发了 v1.4.0：

- 页面：`https://github.com/jinhui19200/warehouse-management-system/releases/tag/v1.4.0`
- 附件：`warehouse-manager-1.4.0-arm64.dmg`（134.5 MB）、
  `warehouse-manager-1.4.0-setup.exe`（106.5 MB），均为 `state=uploaded`
- 校验：**本地整份 sha256 与 GitHub 服务端 `digest` 逐字节一致**（见下）

**发布时踩的坑**：用 `urllib` 直传 134.5 MB 的 DMG，传到 13 分钟时
`BrokenPipeError: [Errno 32] Broken pipe` —— 实测直连只有约 165 KB/s，
长时间大文件传输会被掐断，而且 `stdout` 被缓冲，进程被杀时连「传到哪一步」
都看不到（改用 `python3 -u` 才有输出）。
换成 `gh release upload` 一次就过，**3 分 8 秒**传完 241 MB（约 1.3 MB/s）。
结论：**发布附件一律用 `gh release upload`，别自己用 urllib/curl 裸传大文件。**

（另：`urllib` 那次失败前已经建好了 Release 壳子，所以重传前要先确认
`assets` 是空的、别把半截文件当成已上传 —— 附件状态看 `state` 是否为 `uploaded`。）

**把「发布」固化成脚本后，又被自己的校验误报了一次（2026-09-23）**

发布这一步靠人记是记不住的，于是写成 `publish-release.sh`（存在技能
`electron-release-packaging` 里）。第一次真跑，五步走了四步半 ——
最后一步 `curl` **退出码 92**（`CURLE_HTTP2_STREAM`，HTTP/2 帧层流错误），
脚本当场判失败。但两个包其实早已在服务端字节一致：那是**网络抖动，不是发布出错**。

这正是最糟的失败模式 —— **一个会随机变红的校验，用两次就没人信了**。
校验必须只对**真实缺陷**变红。于是重构成两条职责分明的判据：

| 判据 | 证明什么 | 网络抖动时 |
| --- | --- | --- |
| 比服务端 `digest` 的整份 sha256 | 服务端**存对了** | 不受影响，该红就红 |
| 下前 1 MB 比哈希（`--http1.1 --retry 3`） | 下载路径**真的在发字节** | **只警告，不判失败** |

`digest` 是 GitHub 给每个 release asset 算的**整份** sha256，拿它跟本地
`shasum -a 256` 比 —— 覆盖整份文件、**零下载**，而且顺带省掉了重传：
远端 sha256 已一致就跳过上传（本环境上行慢，白传 241 MB 要好几分钟）。

同一轮还改掉了第 4 步的判据：原来要求「附件数 == 传入文件数」，
于是**没法只更新其中一个平台的包**（重打包 Windows 版会被拦下）。
改成「**点名的文件必须都在且 `state=uploaded`**，多出来的附件只警告」。

三条改动都做了**反向验证**（改坏 → 确认变红 → 改回）：

| 变异 | 期望 | 实测 |
| --- | --- | --- |
| 第 5 步本地哈希改坏 | 红在第 5 步、退出码 1 | ✓ 退出码 1 |
| 把 `curl` 换成必然失败的 `false` | 只警告、退出码仍 0 | ✓ 退出码 0 |
| 用一个不存在的文件名查 `digest` | 返回空 → 走上传分支 | ✓ 返回空 |

修完后重跑：**六步全过、退出码 0，几秒跑完**（两个附件都走「已一致，跳过上传」）。

**踩到的坑：右键菜单「闪一下就没」**

菜单挂载后立刻被关掉，而且极具迷惑性 —— 菜单**在 DOM 里确实出现过**
（`MutationObserver` 能记到 1 次），但任何轮询都抓不到，看起来就像「右键没反应」。

根因：React 对离散事件（`contextmenu` 属于此类）会**同步 flush**，
于是 `ContextMenu` 的 `useEffect` 是在 `contextmenu` **还在往 window 冒泡的过程中**跑完的 ——
此时新注册的 window 监听器立刻收到**同一个** `contextmenu` 事件，把刚挂上的菜单关掉。

改法：关闭监听一律用 **`pointerdown`**，它在 `contextmenu` **之前**就发生过了，
不存在「在途事件」；右键点别处也照样能关（pointerdown 先关旧菜单，紧接着的 contextmenu 再开新的）。

## 2026-09-22

### 仓库页搜索置顶、仓库台账导出、经手人/领取人、报表每卡独立时间窗（v1.3.0）

四项改动一次做完。

**仓库页：搜索命中的物品置顶（其余照常显示）**

- 右上角加搜索框。匹配名称的物品**排到最前面**，其余物品**保持原有顺序跟在后面**
- 刻意**不隐藏**未命中的行：仓库页是全量台账，用户搜「螺丝」时往往还要顺手核对旁边的库存；
  把其余行藏起来就得反复清空搜索框才能看全。这是需求里明确选定的方案（另一个选项是筛选隐藏）
- 命中的行**名称格加浅蓝底 + 加粗**，整行再铺一层极淡的蓝 ——
  光靠「排到前面」看不出到底哪几行是命中的
- 配色刻意避开红/橙：那两个色已经被「负库存」和「低于警戒值」占住了。
  命中是中性信息，用同色系会让人误以为这些物品也出了问题
- 顶部计数在搜索时变成「匹配 N / 共 M 种」；一个都没命中时明说一句
  「没有名称包含「xxx」的物品，下面是全部 M 种」，而不是默默显示全部
- **重排用「filter 分两段再拼接」，不用 `sort`**：`sort` 的比较函数返回 0 时**不保证稳定**
  （V8 对超过 10 个元素会换用不稳定的分支），命中项与其余项各自内部的顺序会随机打乱，
  每敲一个字符整张表都在跳
- 纯函数 `pinMatches` / `matchesItemQuery` 放在 `src/shared/utils.ts`，数据层可直接测

**仓库页：导出 Excel**

- 新增「导出 Excel」按钮，走**已有的** `export:xlsx` 通道（那个通道本来就是通用的，不必新开一条）
- 导出**界面当前的顺序**（命中的在前），列：名称 / 数量 / 单位 / 警戒值 /
  本月入库（9月）/ 本月出库（9月）/ 状态
- 「状态」列是把界面上「低于警戒值的行标红」这条信息落成文字 ——
  导出的文件里没有颜色可看，不落成一列的话这条信息就丢了

**操作页：入库加「经手人」、出库加「领取人」（与「操作人」并存）**

- 一条记录里存**同一个 `handler` 字段**，语义随方向变化：入库是经手人、出库是领取人。
  刻意不用两个字段 —— 一条记录非入即出，两个字段必然一个恒为空，
  查询、导出、断言都要处处判断「该看哪个」
- 与 `operator` 并存：操作人是「谁在系统里录的账」，`handler` 是「货实际交给了谁」，
  可以是同一个人，也可以不是
- 标签文案由 `handlerLabel(type)` 统一给出，**表单标签 / 记录列表表头 / 导出列名三处取自同一处定义**，
  改文案不会漏掉某一处
- 记录列表新增「经手人/领取人」列（列名把两种叫法都写出来，
  用户看到「领取人：王五」不会以为列名错了）
- 撤销确认框里带上交接人 —— 撤销是反向冲销库存的破坏性操作，
  确认框里要能看出「货交给了谁」
- **自动补全只在本方向的记录里找**：入库建议历来的经手人，出库建议历来的领取人。
  两个角色在业务上是分开的（一个把货交出去、一个把货领走），
  拿领取人的名字去建议经手人会让人选错
- 提交后保留输入（同一批货通常由同一个人经手）；
  但**切页签会让表单重新挂载，保留只在同一次挂载内成立**
- 旧数据文件里的记录没有 `handler`，在 `parseDB` 里统一补空串（与当年补 `operator` 同一处）

**报表页：每张卡的时间窗口相互独立**

- 原先所有卡片**共用**一个时间窗口，理由是「各滑各的就没法横向对比」——
  实际用起来正好相反：不同物料的周转节奏差得很远，A 物料三个月清空、B 物料一年才动一次，
  共用一个窗口意味着想看 B 的历史就得把 A 一起拖进空白月份
- 现在 `offset` 存在每张卡内部，卡片自带控制条
  （`◀ 更早` / 区间 / `更晚 ▶` / `回到最新`），拖动也挂在各自的 `.report-plot` 上
- **可滑范围也按各自的记录算**：滑到「这个物品」最早一条记录所在月为止。
  沿用全局范围的话，一个刚建的物品也能被滑到十几年前的空白窗口，而它自己没有任何历史可看
- 页面级工具条随之去掉，操作提示改成「每张图表可各自按住左右拖动调整时间，互不影响」
- 卡片内控制条的下外边距（22px）**不是普通留白**：入库柱的数值标注标在柱子**上方**，
  最高那根会溢出绘图区，这段外边距是给它留的位置。改小会让标注压到「更早 / 更晚」按钮上

**验证**

- 五层全绿：类型检查 0 错误，数据层 **100**（+18）/ 端到端 **62**（+3）/
  界面 **201**（+56）/ 真应用 **41**（+1）
- **反向验证过**（新断言必须能变红，否则等于没测）：
  - 把 `pinMatches` 改成「筛选隐藏」→ **5 条搜索断言立刻变红**
    （行数从 6 掉到 2、置顶顺序消失、搜不到时行数变 0）
  - 把 `offset` 提到页面级（恢复共用窗口）+ 可滑范围改成全局 →
    **3 条 ★ 独立性断言变红**（另一张卡跟着滑到了 2025-09~2026-08、
    闲置物料的「更早」从禁用变成可用、拖动后另一张卡也动了）
- 两处断言写法上的坑，顺手修掉：
  - 操作页两个表单**同时在 DOM 里**，用 `document.querySelectorAll('.tx-suggestion')` 全局查补全提示
    会把另一个表单的提示也收进来，「出库不该建议入库经手人」这条会假失败 → 改成按表单下标取
  - 撤销确认之后代码还会弹一个「库存变为负数」的 alert，它**同样是一条 dialog**，
    于是 `dialogs.at(-1)` 指向的是那个 alert 而不是确认框 → 改成按类型把 confirm 挑出来
- 记录页那两处 `cols[5]` / `cols[6]` 的按下标断言，改成按表头名取 ——
  加「经手人/领取人」这一列正好会把它们顶到别的列上，而报出来只会说「内容不对」

**打包 v1.3.0（本轮起只出 arm64 + Windows 两个平台）**

| 产物 | 字节数 | SHA256（前 16 位） |
| --- | --- | --- |
| `warehouse-manager-1.3.0-arm64.dmg` | 141,025,600 | `a46c94484ca65288` |
| `warehouse-manager-1.3.0-setup.exe` | 111,699,425 | `58db5f10f916c424` |

- `electron-builder.yml` 的 mac 目标**去掉 x64**，只留 arm64。
  本机是 Apple Silicon，x64 那份要跑 Rosetta 才验得了，等于每次多打一份自己验不了的包
- 打包产物自检 `verify:packaged` 在 arm64 DMG 上 **40/40 通过**（含经手人/领取人落盘、
  以及「拖动一张卡时另一张卡不受影响」这条 ★ 独立性断言）
- `app.asar` 哈希与 DMG 内的完全一致，asar 清单干净（只有两个依赖包 + `out/` + `package.json`）

**打包时踩到的两个环境坑（都不是代码问题，但会让打包看起来像坏了）**

- **`.asar` 文件的写入被拦**，报 `Brokered file token refused: modify backup failed`。
  这个报错极具误导性 —— 它出现在「解压 Electron 的 win32 zip」这一步，
  看起来像下载损坏或磁盘问题。实际原因是环境往每个 node 进程注入了
  `NODE_OPTIONS=--require=.../node-language-shim.cjs`，那层代理会对 `.asar` 做校验并拒绝。
  同目录写 `.txt` / `.zip` / `.7z` 全正常，只有 `.asar` 不行，这才是分诊的关键
  - 解法：`env -u NODE_OPTIONS npx electron-builder --win --x64 --publish never`
  - 注意 **`env -u NODE_OPTIONS` 只对 node 有效**，shell 里的 `unzip` 等不受影响；
    另外提升沙箱权限（escalation）**不能**绕过它 —— 代理是注入进进程的，跟沙箱级别无关
- **`ELECTRON_RUN_AS_NODE=1` 会让打包好的应用起不来**，报一串
  `bad option: --no-sandbox` / `bad option: --disable-gpu`。
  原因是 Electron 二进制被当成 Node 跑，于是所有 Electron 参数都成了「非法选项」。
  `verify-packaged.mjs` / `verify-app.mjs` 原本只清了 `NODE_OPTIONS`，
  这次把 `ELECTRON_RUN_AS_NODE` 一起清掉（设成空串即可，Electron 按真值判断）
  - 顺带一提，这个坑正是「只出 arm64」的另一个理由：x64 包在 Apple Silicon 上
    更容易撞上这条路径，而它又不是我们要交付的东西

## 2026-09-20

### 装上 GitHub Actions：打 tag 自动出 mac / win 安装包

**做了什么**：仓库里加了 `.github/workflows/build.yml`（内容与 `ci/github-actions-build.yml`
一致）。`verify` 作业在 ubuntu 上跑类型检查 + 数据层 + 界面三层自检；`build-mac` /
`build-win` 只在推 tag 时跑，各自把安装包传成 Artifact。

**工作流文件为什么另存一份在 `ci/`**：推送 `.github/workflows/` 下的文件需要 token 具备
`workflow` scope，而本机凭据实测只有 `gist, read:org, repo`。所以工作流是靠网页 UI 贴进去的，
`ci/` 那份留作可编辑的副本。**注意**：仓库里**存在** workflow 文件并不妨碍推送其他文件
（已实测），只有真要改工作流本身才需要那个 scope。

**踩到的坑：安装包打成功了，却因为「发布」失败而整体挂掉**

第一次 tag 构建（run #3）`verify` 全绿，但 `build-mac` / `build-win` 都红。拉日志发现
安装包**其实都产出了**：

```
• building  target=DMG   arch=arm64  file=dist/warehouse-manager-1.2.0-arm64.dmg
• building  target=DMG   arch=x64    file=dist/warehouse-manager-1.2.0-x64.dmg
• building  target=nsis              file=dist\warehouse-manager-1.2.0-setup.exe
⨯ GitHub Personal Access Token is not set, neither programmatically, nor using env "GH_TOKEN"
```

electron-builder 检测到「在 tag 上跑」就**隐式去发 GitHub Release**，没有 token 直接报错
退出 —— 把已经成功的构建结果一起带挂，`upload-artifact` 那步也因此被跳过，最后一无所获。
日志里它自己给了提示：`Implicit publishing triggered by git tag. This behavior will be
disabled in electron-builder v27.`

**改法**：`build:mac` / `build:win` 各加 `--publish never`，把「构建」和「发布」拆开 ——
构建永不发布；将来要发 Release 就在工作流里单独加一步 `gh release create`
（写法记在 `ci/github-actions-build.yml` 头部）。

**顺带修的**：把 `esbuild` 声明成显式 devDependency。`verify:store` 与
`preview/build-preview.mjs` 都直接调它，之前只是靠 vite / electron-vite 传递带进来的 ——
验证基建不该依赖「碰巧」。

**验证 CI 产物：不只看「体积对」，要证明「内容一致、真能跑」**

重新推 tag 后 run #4 三个作业全绿。但「绿了」只说明构建成功，**不说明打出来的东西和本地一致**。
把两个 Artifact 下下来逐项核对（mac 261MB、win 111MB）：

```
app.asar SHA256   03776bb79f9a47a2e31575282b81c2f6…   ← 本地 DMG 与 CI DMG 完全一致
app.asar 大小     1,370,417 字节                      ← 两边完全一致
.app 未压缩体积   289 MB                              ← 两边完全一致
```

**内容逐字节相同，差异全在 DMG 容器本身**，而且方向与直觉相反：

| | 本地 DMG | CI DMG |
| --- | --- | --- |
| 卷内 | `Applications` + `.app` | 同左，**外加** `.background.tiff`、`.VolumeIcon.icns`、`.DS_Store` |
| 体积 | 141.8 MB | 127.4 MB（**小 10%**） |

CI 那份**多**了背景图与卷图标（electron-builder 的 dmgbuild 做窗口美化），**却还小 10%** ——
只可能是压缩级别：本地是 `hdiutil create` 绕行脚本打的（默认 zlib 级别），
CI 走 electron-builder 正常路径（更高级别）。**这是沙箱绕行的代价：本地那两个 DMG 缺窗口美化，
打开时是素白 Finder 窗口**，纯外观、功能无影响。

- **CI 的 DMG 真启动验证**：挂载 CI 的 arm64 DMG 跑 `verify:packaged` → **37/37 通过**。
  挂载、启动、渲染、12 个月窗口、左右滑动、回到最新、记录页 33 条、导出编码完成，全过
- **Windows 产物几乎逐字节相同**：CI 的 exe 111,699,079 B vs 本地 111,698,095 B，
  差 **984 字节（0.0009%）**。NSIS 的 exe 本身已 LZMA 压缩、zip 再压不动，所以这个对比有效，
  比 mac 那边的证据还硬
- **本地 asar 清单核对通过**：16 个文件 / 1.30MB，`scripts/`、`preview/`、`ci/`、`*.command`、
  `.workbuddy-ai/` 一个都没混进去

**下载 artifact 的两个技巧**（记在这里免得下次再花二十分钟）

- artifact 是 zip，**中央目录在文件末尾** —— 只发一个 Range 请求取末尾 64KB，就能读出
  里面每个文件的真实大小与偏移。**1.3 秒** vs 完整下载二十分钟。想确认「CI 产出的文件多大」
  根本不用下全量
- 真要下全量时**分块并行**：单连接只有 ~360KB/s，6~8 段并行能到 **2.3MB/s**。
  但签名 URL 会中途失效，每块必须带重试 + 体积校验；另注意 `xargs` 内嵌整段脚本 + URL
  会超命令行长度上限，worker 要写成独立文件、只传索引

> 两个 Artifact **90 天后过期**。要变成永久可下载的 Release 附件，得给工作流加一步
> `gh release create`（写法记在 `ci/github-actions-build.yml` 头部）。

## 2026-09-19

### 补上导出的端到端覆盖：真产出一份能被解析的 xlsx

**背景**：上一条把 `node_modules/xlsx` 从 asar 里排除了（依据是它被 Vite 内联进渲染
bundle），当时只用「构建产物断言」证明 bundle 里带着 xlsx 编码器 —— 因为导出走原生
保存对话框，端到端自动化点不了。这是个**已知的覆盖缺口**，这次把它补上。

**为什么一开始想在打包产物里测、结果没成**

本想在打包后的真 Electron 里把 `window.api.exportXlsx` 换掉、直接截取渲染进程算好的
字节。实测发现做不到：`window.api` 是 contextBridge 暴露的，**frozen + sealed** ——

| 尝试 | 结果 |
| --- | --- |
| `api.exportXlsx = fn` | 静默失败（sloppy mode 不抛错，`try/catch` 报不出问题） |
| `Object.defineProperty(api, 'exportXlsx', …)` | `TypeError: Cannot redefine property` |
| `window.api = fake` | 静默失败（`window.api` 本身也是 non-writable / non-configurable） |

这是 Electron 的安全模型，行为正确，是我选错了地方。

**改在预览版做**：`preview/mock.ts` 里 `window.api` 是**普通对象**（不是 contextBridge），
可以替换；而预览版跑的渲染 bundle 与打包产物**是同一份**。所以在这里截取字节，
确实覆盖了「打包后导出还能不能用」这个风险。

**断言（verify-ui 第 9 节，9 条）**

- 点导出后渲染进程产出了字节、默认文件名是 `出入库记录.xlsx`
- 字节以 ZIP 魔数 `PK 03 04` 开头（xlsx 本质是 zip）
- **拿回 Node 用 xlsx 真解析**：工作表名 `出入库记录`、表头
  `时间/名称/数量/单位/操作人/类型`、数据行数与界面上记录数一致、不是空表

**「长度非零」是无效断言**：反向验证时把工作簿类型临时改成 `csv`，
「产出了字节」这条**照样是绿的**（CSV 文本也是非空字节），
而 ZIP 魔数、工作表名、行数三条立刻变红，失败信息也自证了原因
（`ef bb bf e6` 是 UTF-8 BOM、`Sheet1` 而非「出入库记录」、`表里 -1 行`）。
只查长度不查内容，等于没查。

**打包产物里补的轻量检查（verify-packaged，2 条）**

真 Electron 里拦不了 IPC，所以那一层只验「编码器跑通了没抛」：把可写的 `window.alert`
换掉记录消息，点导出后不应出现「导出异常」；且导出按钮应停在 `导出中…`
（若 `XLSX.write` 抛了，catch 会把状态复位、按钮退回「导出 Excel」）。
**字节级校验在 verify-ui 做，两层合起来才完整。**

- 反向验证过：`bookType` 改成 `csv` 时 5 条断言变红，改回后恢复全绿
- 验证规模：类型检查 0 错误；数据层 82 / 端到端 59 / 界面 **151**（+9）/ 真应用 40 = **332**；
  打包产物 arm64 与 x64 各 **37**（+4）通过

### 报表页时间窗口改为 12 个月 + 左右滑动 + 打包内容改白名单（v1.2.0）

**报表页：窗口 6 → 12 个月，可左右滑动**

- 一次显示 12 个自然月，默认贴着当前月（最近 12 个月）
- 平移方式两条：工具条的 `◀ 更早` / `更晚 ▶` 按钮，或**直接在图表上按住左右拖动**
  - 拖动方向按「拖动地图」的直觉：往右拖 = 时间轴往右拉 = 看到更早的数据
  - 用 Pointer Events + `setPointerCapture` 实现，`touch-action: pan-y` 把横向手势收归应用处理
- **平移的是所有卡片共用的同一个窗口**。各卡片各滑各的就没法横向对比了，
  而这张报表的价值恰恰是并排比较不同物料的进出节奏
- 往前最多滑到「最早一条记录所在月」为止（再往前全是空窗口，没有信息量），
  按钮到边界自动禁用，并出现 `回到最新` 复位。窗口外还有记录时明确提示「窗口外还有 N 个月的记录」——
  **两侧都算**，因为往前滑之后更新的月份同样会落在窗口外
- 范围钳制用**派生值**（`safeOffset = Math.min(offset, maxOffset)`）而不是把钳制结果写回 state：
  记录变少（撤销、清空）会让 `maxOffset` 缩水，用派生值就不需要 effect 去纠正，
  也不会出现「state 是 5、界面显示 2」这种不一致
- 新增纯函数 `monthDiff(from, to)`，用「年×12 + 月」算差值。
  **不能对 Date 做减法**：得到的是毫秒，各月天数不同，除以 30 天会算出 0.97 个月，取整后边界月份随机偏一格
- 卡片最小列宽 340 → **520px**。这是被柱上数值标注倒逼的：12 个月要分 12 列，
  340px 时每列约 23px，而「1200」这种四位数标注本身就有约 22px 宽，相邻月份会叠在一起

**断言改造：从「按下标定位柱子」改成「按月份定位」**

- `barsIn[2]` 表示 6 月，只在「窗口 = 最近 6 个月」时成立。窗口一改成 12 个月，
  所有下标断言全部静默错位 —— 不报错，只是测的不是原来那个月份了
- 现在统一走 `barAt(card, '2026-06', 'in')`，窗口月数怎么变都不会错位
- 新增两条几何断言：12 个月份标签与柱子**逐列对齐**（比的是 `plot-col` 中心，因为柱子只占列宽 56%）、
  柱子在各列内居中。12 列下肉眼看不出 flex 布局被改坏，只能量 —— 实测偏差 **0.0px**
- 反向验证过拖动断言能变红：临时把 `PX_PER_MONTH` 从 60 改成 120，断言正确报出只前移 1 个月，随后改回

**打包内容从黑名单改成白名单（重要，三轮 release 一直存在的问题）**

- 原配置只写了 7 条排除项，等于「其余全收」。后果是 **1.0.0 / 1.1.0 / 1.2.0 三个版本的
  安装包里都塞进了开发期产物**：
  - 1.6MB 的验证截图（`out/verify-ui/*.png`、`out/report-*.png`）
  - 39KB 的内部工作笔记（`.workbuddy-ai/memory/*.md`）
  - `scripts/`（含全部断言源码）、`preview/`、`ci/`、两个 `*.command` 启动脚本
- 黑名单的根本问题：**它只能挡住已知的目录**。验证截图是后来才生成的，没人想到要补一条排除，
  于是就这么漏了三轮。白名单反过来 —— 没列出来的一律不进，以后新增开发目录默认是安全的
- 顺带排除 `node_modules/xlsx`：`xlsx` 是渲染进程 `import` 的，会被 Vite 整个打进渲染 bundle，
  已核实 bundle 里**零个 `require()`**、main / preload 都没引用它 —— asar 里那份是死重
- asar 从 **11.6MB 降到 1.3MB**（16 个文件：`out/main` + `out/preload` + `out/renderer` +
  两个运行期真的 `require` 的 `@electron-toolkit/*` + `package.json`）
- 敢删 xlsx 是因为加了守门断言（`verify-ui` 第 10 节）：bundle 里必须带着 xlsx 编码器
  （`SheetJS` + `xl/workbook` 标记），且不得 `require("xlsx")`。
  哪天真把导出挪到主进程，这条断言会先变红，而不是打包后静默失效 ——
  导出走原生保存对话框，端到端自动化点不了，没有别的断言能兜住

**新增验证层：`npm run verify:packaged`**

- 从 DMG 挂载卷（或 `dist/mac*` 的 `.app`）**真启动打包好的应用**，用 CDP 连真实窗口验证报表页。
  这是唯一一层在「打包后的东西」上跑的验证，另外三层跑的都是 `out/`
- 上述打包内容问题就是它挖出来的，前三层都看不见
- 33 项断言，含 12 个月窗口、柱子在轴两侧、标注位置与数值、比例、平移与拖动
- 有个坑值得记：**操作页没有时间输入框**，界面录入的记录时间都是「现在」，
  于是全新 userData 里 `maxOffset = 0`，平移被钳制锁死 —— 那时「更早」禁用、区间纹丝不动，
  整组平移断言会在「无事发生」的状态下通过。所以脚本先往磁盘铺 15 个月历史，
  并加了一条守门断言「『更早』按钮可用」。`SEED_MONTHS=1` 可复现反向验证，那时 13 条会变红

**版本号 1.1.0 → 1.2.0**

**产物（均含 12 个月窗口与滑动）**

| 文件 | 大小 | SHA256（前 16 位） |
| --- | --- | --- |
| `warehouse-manager-1.2.0-arm64.dmg` | 141,810,456 B | `20a29ecd1a7e46fa` |
| `warehouse-manager-1.2.0-x64.dmg` | 149,271,123 B | `63ea831e4a74977b` |
| `warehouse-manager-1.2.0-setup.exe` | 111,698,095 B | `8a024f08265a5ab5` |

- 三个包都**挂载后真启动验证过**：arm64 与 x64 各 **33/33** 通过；Windows 包改用 asar 核对，
  `report-range` / `range-btn-reset` / `report-toolbar` / `plot-idle` 全部命中，
  且 `.workbuddy-ai`、验证截图、`node_modules/xlsx` 均已清除
- 体积只降 2~3%（UDZO 压缩后差异被抹平），真正的收益是**安装后占用**：asar 11.6MB → 1.3MB
- 四层验证全绿：类型检查 0 错误，数据层 82 / 端到端 59 / 界面 142 / 真应用 40 = **323** 项

### 柱子加数值标注 + 版本号提到 1.1.0 + 重新打包

**柱上数值标注**

- 报表页每根柱子直接标出数量，不用再靠鼠标悬停猜：**入库标在柱顶上方、出库标在柱底下方**
- 刻意标在柱子**外面**而不是里面：低于 2px 的短桩（值很小但非零）根本塞不下字，
  标在里面要么被裁掉、要么得把短桩撑高 —— 而撑高就等于撒谎，柱子高度不再代表数量
- 位置用 `bottom/top: 100%` 贴着**自己那根**柱子。若改成统一飘在某一行上，
  短柱的数字会离柱子很远，读不出对应关系
- **值为 0 的月份不标注**：6 个月 × 上下两半，满屏的「0」比不标更难看
- 标在柱子外面就有溢出：最高那根（满刻度 92px）的标注会溢出绘图区约 12px，
  靠 `.report-head { margin-bottom: 22px }` 和 `.plot-labels { padding-top: 20px }` 让位。
  **这两处不是排版留白，改小会压字**（verify-ui 有断言盯着）

**窗口内零出入库的边界**

- 物品在最近 6 个月完全没有出入库时（慢周转物料，正常状态），
  卡片原本会显示刻度 **`1 / 0 / 1`** 而一根柱子都没有 —— 一个空图表标个「1」是纯噪音，
  看着还像渲染坏了
- 根因：`max = Math.max(1, ...)` 把「避免 0 除」和「显示刻度」混用了。
  现在拆开：`peak` 是真实峰值（用于显示），`max = Math.max(1, peak)` 只用于避免 0 除
- 没有出入库时不显示刻度数字，改为在绘图区正中盖一句「近 N 个月无出入库」。
  **不隐藏整张图**：卡片高度保持一致，用户也能一眼分辨「这个物品存在但没动过」
  和「图表没画出来」—— 横轴月份照常显示
- 预览数据里新增一个「闲置物料 X」长期留在种子数据里，专门覆盖这条分支。
  它的库存刻意设为**高于**警戒值，让「零出入库」与「低于警戒值」两个状态保持正交，
  否则它会连带扰动所有跟低库存有关的计数断言，排查时容易误判

**版本号 1.0.0 → 1.1.0**

- 上一版（没有报表页）也叫 1.0.0，文件名完全一样，发给别人时分不清新旧。
  现在产物名变成 `warehouse-manager-1.1.0-*`，旧文件保留不删

> 后续（同日）：经确认已清理 `dist/`，删掉 1.0.0 / 1.1.0 的六个安装包与中间产物
> （约 1.75GB），现在只保留 1.2.0 三个安装包。上表与 1.0.0 的表仅作历史记录，
> **文件已不在 `dist/` 里**。

**产物（三个都重新打过，均含报表页、数值标注与零出入库处理）**

| 文件 | 大小 | SHA256（前 16 位） |
| --- | --- | --- |
| `warehouse-manager-1.1.0-arm64.dmg` | 139 MB | `236ba6fb6cffc7d3` |
| `warehouse-manager-1.1.0-x64.dmg` | 147 MB | `2eb9e9dc30de7146` |
| `warehouse-manager-1.1.0-setup.exe` | 109 MB | `40dfee9715b3db6e` |

> 这三个包在加入「零出入库」处理前打过一版（`5c90207e…` / `744a0ba7…` / `972f3087…`），
> 因渲染代码有改动，已重新打包覆盖。上表是当前产物。

- 两个 mac 包内 `Info.plist` 的 `CFBundleShortVersionString` / `CFBundleVersion` 均为 `1.1.0`，
  asar 内 `package.json` 也是 `1.1.0`（Windows 包同样核对过）
- **从 DMG 挂载卷真启动打包产物，arm64 与 x64 各 18/18 通过**：资源确实从 `app.asar` 加载、
  preload 链路通、经真实界面造数据后报表卡片数与物品数一致、
  入库柱在轴上 / 出库柱在轴下、标注位置与数值都对、每根柱高与数值成比例、
  最高柱标注未压到卡片头部
- Windows 包无法在本机运行，改用 asar 内容核对（asar 是明文拼接，能直接证明打进去了）：
  `统计区间`、`plot-value`、`value-in`、`value-out`、`警戒值`、`入库（轴上方）` 全部命中

**验证**：类型检查 0 错误；数据层 **71**、端到端 **59**、界面 **124**（+16）、真应用 **40**，共 **294** 项全过。

### 补掉报表断言里「空集恒真」的洞 + 真启动 x64 打包产物验证

- **发现一个会静默失效的断言**：报表页那两条「入库柱全部在零轴上方 / 出库柱全部在零轴下方」
  写成了 `bars.filter((b) => b.h > 0).every(...)`。`[].every()` 返回 `true` ——
  柱子要是因为回归全没了，过滤结果是空数组，**断言照样绿**。
- 修法：把「确有非零柱」独立成一条断言（失败信息带 `${非零}/${总数}` 计数），再做全称判断。
  界面自检 **106 → 108** 项。
- **验证过这条新断言能变红**：在预览页把 `.plot-bar` 高度全清零，对同一份 DOM 跑新旧判据，
  旧写法 `{"nonZero":0,"oldWay":true}`（漏判）、新写法 `{"nonZero":0,"newWay":false}`（正确报错）。
  这个对照实验不用重新构建，比改源码重建快得多。
- **补上 x64 打包产物的启动验证**：此前只对 arm64 的 DMG 做过 CDP 验证，x64 那份仅 grep 过 asar。
  现在从 DMG 挂载卷真启动 x64 的 app（走 Rosetta），经真实界面入库 2 笔、出库 1 笔后看报表，
  **13/13 通过**：资源确实从 `app.asar` 加载、preload 链路通、页签正确、
  2 张卡片对应 2 个物品、入库柱在轴上 / 出库柱在轴下、柱高成比例（入库 500→92px、出库 200→37px）。
- 两个自查时的自我纠正，一并记下免得再犯：
  - 断言「资源路径指向 app.asar」读的是 `getAttribute('src')`，拿到的是相对路径
    `./assets/…`，**恒为假** —— 这是个假失败。要读解析后的 `el.src`。
  - 用全新空白 userData 启动时仓库里没有物品，报表页走的是**空状态分支**，
    图表断言全都在空集上「通过」。必须先通过界面造数据，否则等于没测。

### 新增「报表」页：每个物品的月度出入库双向柱状图

- 新增页签「报表」（放在「操作」之前，录入口留在最右边）
- 仓库里**每个物品一张卡片**，卡片内是以**月为横轴、数量为纵轴**的柱状图：
  **入库柱在零轴上方（绿）、出库柱在零轴下方（红）**
- 为什么是上下双向而不是并排双柱：入库和出库在语义上是一进一出两个方向，
  共用一条零轴能直接读出净变化 —— 上下差不多长是进出平衡，上长下短是在囤货。
  并排双柱要来回比较两条不同的基线，反而费劲
- **上下两半共用同一刻度**（取该物品所有月份里的最大值）。若各自按自己的最大值缩放，
  一个「入库 10、出库 1000」的物品会画成两根一样长的柱子，是错的
- 窗口为最近 **6 个自然月**，无数据的月份补 0 而不是跳过 —— 跳过会让横轴变成
  「有数据的月份」，不同物品的卡片月份错位，没法横向对比
- 卡片头部显示该物品 6 个月的入库/出库合计；页面顶部标出统计区间与图例
- 有更早的历史数据时明确提示「另有 N 个月的更早数据未显示」，不静默丢弃

**新增纯函数（`src/shared/utils.ts`，数据层可直接测）**

- `recentMonths(count)` —— 最近 N 个自然月，从早到晚。用 `new Date(y, m - i, 1)`
  逐月回推并固定为 1 号：月份为负数时 Date 自动向年份借位（跨年不用特判），
  固定 1 号则避免「31 号回推」落到不存在的日期上
- `monthlySeries(records, itemId, months)` —— 某物品在各月的出入库合计，
  返回**定长**数组与传入月份一一对应，界面直接按下标画柱，不必判断某月缺不缺数据
- `monthLabel(month)` —— `2026-09` → `9月`

**图表用 CSS Grid 而不是 SVG**

卡片宽度随窗口变化，SVG 用固定 viewBox 会让文字跟着缩放（宽窗口字太大、窄窗口太小）。
Grid 定义「入库区 / 零轴 / 出库区 / 月份标签」四行 + 「刻度 / 绘图区」两列，
刻度按行自动对齐，不用手工算偏移。

**预览种子补了 6/7/8 月的历史数据**

原本 9 条记录只覆盖 8、9 两个月，报表页 6 个月里只有 2 个月有柱子，看不出效果。
补了 8 条（避开 08-20 与 09-17~09-19 —— 记录页的日期筛选断言依赖那几天的条数）。
记录页的条数断言随之校准：共 17 条、入库 11 条、出库 6 条。

验证：数据层 **71**（+11）、端到端 **59**、界面 **108**（+18）、真应用 **40**，共 **278** 项全过。
报表页断言包含**几何判定**（入库柱底边不低于零轴、出库柱顶边不高于零轴）、
**共用刻度**（上下两个刻度标签相等）、**柱高与数值成比例**（300 的柱约为 150 的两倍）。

### 验证脚本改为按表头名取单元格 + 修表单时间默认值过期

**验证脚本不再用下标取单元格**

- `verify-ui` / `verify-electron` / `verify-app` 里取单元格原本按**下标**，
  加一列就会**静默错位**。新增「警戒值」列时就是这样让 4 处断言同时失效的，
  而且报出来的是「值不对」，看不出根因是列错位，排查绕了不少弯
- 改为按**表头名**取值，返回 `{ 表头名: 单元格值 }`：
  - 表头名本身会随内容变化（`本月入库（9月）` 带月份、`单位（已锁定）` 带锁定标记），
    统一去掉括号内容作为规范名，跨月也不会失效
  - 含 `<input>` 的单元格（警戒值）直接返回 `.value`，
    把「`td.textContent` 恒为空串」这个坑收进读取函数里，调用方不必再记
- 失败信息从单个值改为整行（`名称=… / 数量=…`），一眼能看出到底取到了什么

**操作页表单时间默认值不再过期**

- 默认时间是表单**挂载那一刻**取的。停在操作页跨过零点后，默认值就成了昨天的日期
- 改为：用户**未手动改过**时间时，一开始操作表单（聚焦）就重新取「此刻」
- 刻意**不做**「提交时才取当前时间」：那样屏幕上显示 09:00、记进流水却是 10:00，
  显示与结果不一致，比默认值略旧更难排查。现在两者始终一致
- 提交前必须输入名称和数量，必然触发聚焦，因此不需要额外的定时器兜底
  （渲染层也确实没有任何定时器）

验证：数据层 **60**、端到端 **59**、界面 **93**（+3）、真应用 **40**，共 **252** 项全过。
新增的 3 项用**假时钟**造出「跨零点」场景（否则只能干等一天，等于没覆盖），
并且已实测**去掉修复后其中一项会变红**（读到 `2026-09-19T23:58`），确认断言真的有效。

### 打包 v1.0.0：macOS（arm64 + x64 DMG）+ Windows（NSIS）

产物（均在 `dist/`，**已含「报表」页**）：

| 文件 | 体积 | SHA256（前 16 位） |
| --- | --- | --- |
| `warehouse-manager-1.0.0-arm64.dmg` | 139 MB | `b25c3ad9e4391284` |
| `warehouse-manager-1.0.0-x64.dmg` | 147 MB | `cdeec12b20085086` |
| `warehouse-manager-1.0.0-setup.exe` | 109 MB | `c6b095d49fa9d320` |

> 首轮打包（137/145/108 MB）不含报表页，已在新增报表页后重新打包覆盖，上表为当前产物。
> 版本号当时保持 `1.0.0` —— 按「更新在现在所有版本中」的要求，不额外升版本。
> **后续已改为 `1.1.0`**（见上方「柱子加数值标注 + 版本号提到 1.1.0」），
> 原因是这一版与 1.0.0 文件名完全相同，分发时分不清新旧。上表三行是历史记录，不再是最新产物。

**修了一处会让安装包「打不开」的配置问题**

- 本机没有任何 Developer ID 证书（`security find-identity` 返回 0 个），
  electron-builder 找不到证书就**直接跳过签名**。后果不是「少个签名」这么轻：
  arm64 包只有 Electron 二进制自带的 linker 签名、bundle 本身无签名
  （`Identifier=Electron`、`Sealed Resources=none`），
  别人从网上下载后会被 Gatekeeper 判为**「已损坏，无法打开」**，
  而「已损坏」连「右键 → 打开」都绕不过去，只能敲 `xattr -d` 命令
- 在 `electron-builder.yml` 里加 `mac.identity: '-'` 走 **ad-hoc 签名**。
  签名后 `Identifier` 变成自己的 `com.jinhui.warehouse-manager`、
  `Sealed Resources version=2 files=10`、`codesign --verify --deep --strict` 通过。
  至少能走「右键 → 打开」放行，而不是被判损坏
- 顺带确认 entitlements 里带了 `com.apple.security.cs.disable-library-validation`
  —— 缺这一条，ad-hoc + hardened runtime 的 Electron 应用会因库校验**启动即崩**

**DMG 生成方式**

- `electron-builder --mac` 的 dmg 步骤在命令沙箱里跑不通：
  `dmgbuild` 会挂载卷再把 `.app` `ditto` 进去，而沙箱拒绝写 `/Volumes/...`
  （`file-write-unlink` 被拒 500+ 项）
- 改成 `.app` 先用 `electron-builder --mac --dir` 产出（含签名），
  再用 `hdiutil create -srcfolder` 直接生成 DMG（不挂载卷），
  卷内布局：`库存管理系统.app` + 指向 `/Applications` 的软链接
- 往暂存目录放 `.app` **必须用 `ditto`，不能用 `cp -R`**：`.app` 被签名后文件上带了
  `com.apple.cs.*` 扩展属性，`cp -R` 复制它们会被拒绝（`app.asar: Operation not permitted`），
  而且这个失败**是静默的** —— 复制不完整，DMG 却照样生成成功。
  已在打包脚本里加了体积与签名两道校验卡住它
- Windows 包**不需要 Wine**：electron-builder 26 会自己下载 NSIS 3.0.4.1.7
  在 macOS 上直接出 `.exe`，实测 56 秒完成

**打包后实测**

- 从 arm64 DMG 挂载 → `ditto` 复制出来（`cp -R` 会因签名扩展属性 `com.apple.cs.*` 报
  `Operation not permitted`，必须用 `ditto`）→ 签名仍有效
- 启动打包后的应用，12 秒后进程存活；`SingletonLock` 与 Chromium 配置目录生成，
  说明单实例锁与渲染进程都正常
- 用 CDP 连上真实窗口读 DOM：标题、导航、表头「名称/数量/单位/**警戒值**/本月入库/本月出库/操作」、
  5 行数据、3 行低于警戒值标红、表头汇总「3 种低于警戒值」—— 全部正确

**待办（需要证书才能做）**

- Windows 安装包未签名，用户首次运行会撞 SmartScreen 警告
- macOS 未做公证（notarization），首次打开仍需右键放行

### 仓库页新增「警戒值」+ 应用图标换成用户提供的图

**警戒值（每个物品一个，默认 100，可改）**

- `Item` 新增 `threshold` 字段，默认 `DEFAULT_THRESHOLD = 100`。
  旧数据文件没有这个字段，在 `parseDB` 里统一补齐 —— 补齐点只有一处，
  界面和数据层不可能对同一个物品算出不同的警戒状态
- 仓库页在「单位」之后新增「警戒值」列，单元格是个**可编辑数字输入框**：
  平时长得像纯文本，鼠标移上去/聚焦才显出边框
- **库存低于警戒值时，数量单元格显示浅红**（`#fdeceb` 底 + 红字加粗），
  同时整行给一层极淡的粉底，长列表里一眼能扫到；卡片标题显示「N 种低于警戒值」
- 输入框是**非受控式提交**：输入时不落盘，失焦或回车才提交。
  否则输入「1000」会触发 4 次原子写 + 4 次全窗口广播，
  中途的「1」「10」「100」还会被当成合法警戒值短暂生效，界面会闪
- 非法输入（清空 / 负数 / 非数字 / 溢出）**回退到上一次生效的值**，不提交。
  刻意不做「清空 = 恢复默认 100」：用户清空多半是想取消这次修改，
  静默替他改成 100 是越权改数据。数据层仍保留 100 兜底，防的是绕过界面直接调 IPC
- **改警戒值不写流水**：警戒值是观察阈值，不是库存变动。
  记进出库记录会让「本月入库/出库」统计和撤销逻辑全部失准
- 新增 IPC 通道 `db:setThreshold`（含物品不存在、值未变化时的短路）

**应用图标**

- `build/icon.png` 换成用户提供的仓库图标（1280×1280 JPG → 1024×1024 PNG）
- 原图右下角有「豆包AI生成」水印，按图标本体的包围盒重新裁切后水印残留为 0
- 按 macOS 图标比例加了圆角（半径 22.4%），直角方块在 Dock 上很突兀
- 开发模式下 Dock 显示的是 Electron 自带图标，新增 `applyDevDockIcon()`
  用 `app.dock.setIcon()` 补上；打包后仍用 `.app` 自带图标

**导出行为（确认，未改动）**

- 记录页**默认导出全部**；一旦有筛选（搜索 / 类型 / 时间范围）就导出筛选结果，
  按钮上标明条数（如「导出 Excel（3 条）」），标题显示「筛选出 3 / 共 9 条」

验证：数据层 **60** 项（+15）、端到端 **59** 项（+1）、界面 **90** 项（+25）、
真应用 **40** 项（+6），共 **249** 项全过。

### 真应用自检 `verify:app`（第四个验证层）

- 新增 `scripts/verify-app.mjs` 与 `npm run verify:app`：启动 `out/main/index.js`
  —— **就是双击时跑的那个入口** —— 再用 CDP 连上它的**真实窗口**驱动，34 项断言 / 6 组
- 补上此前唯一没验过的一环：**「双击之后应用真的能打开吗」**。
  `createWindow()` 里的 `join(__dirname, '../preload/index.js')` 这类路径写错**不会报错**，
  窗口照开、只是 `window.api` 是 undefined、界面一片空白 —— 只有真跑真实入口才发现得了
- 覆盖：真实入口启动、窗口标题、加载的是打包产物、preload 注入、IPC 往返、
  界面录入 → 真实落盘、负库存走真实 alert、**真实重启后数据是否还在**、
  真实窗口里的撤销与反向冲销、控制台零错误
- 隔离措施：`--user-data-dir` 指向临时目录（**绝不碰真实应用数据**）、
  `--window-position=-3000,-3000` 把窗口挪到屏幕外（不打断手头的事）、
  跑完即杀进程并删临时目录
- 开关必须放在**脚本路径之后**：放前面会被 Electron 自己的参数解析器拒掉
  （`bad option: --no-sandbox`）；Chromium 解析整条命令行，放后面照样生效
- `VERIFY_APP_SHOTS=1` 可存截图到 `out/verify-app/`（窗口在屏幕外也能截页面内容）

四层验证的分工：

| 脚本 | 覆盖链路 | 项数 |
|---|---|---|
| `verify:store` | 数据层（纯 Node 直接调 store） | 60 |
| `verify:electron` | 端到端（隐藏窗口跑真 Electron：IPC + 磁盘） | 59 |
| `verify:ui` | 界面（真实浏览器：布局几何 + 交互） | 90 |
| `verify:app` | 真应用（真实入口 + 真实窗口 + CDP 驱动） | 40 |

### 界面自检 `verify:ui`（第三个验证层）

- 新增 `scripts/verify-ui.mjs` 与 `npm run verify:ui`：在真实 Chromium 里
  **量布局几何 + 跑交互**，65 项断言 / 8 组。脚本自己管预览服务的启停，
  跑在 8791 端口（避开手动预览的 8765），一条命令从构建到出结论
- 覆盖：首屏与桥接、仓库页表头对齐与列分割线、记录页表头与数据一一对应、
  名称/类型筛选、时间范围筛选（含当日及全部边界）、操作页表单
  （输入框等宽、单位锁定、操作人补全、新建物品、已有物品累加、跨页刷新）、
  撤销反向冲销与确认框内容、控制台零错误
- 构建产物比源码旧时会明确警告，避免「测的是旧代码」
- `playwright` 加为 devDependency，因此不再依赖机器相关的 `NODE_PATH`
- CI 模板的 verify 任务加入 `verify:ui`（需先 `npx playwright install chromium`）

至此三层验证各有分工，互不替代：

| 脚本 | 覆盖链路 | 项数 |
|---|---|---|
| `verify:store` | 数据层（纯 Node 直接调 store） | 45 |
| `verify:electron` | 端到端（隐藏窗口跑真 Electron：IPC + 磁盘） | 58 |
| `verify:ui` | 界面（真实浏览器：布局几何 + 交互） | 65 |

### 表格列分割线 + 表头对齐 + 时间范围筛选

- 修正**表头与数据不对齐**：`.table th`（权重 0-1-1）压过了 `.num`（0-1-0），
  导致「数量」「本月入库」「本月出库」的**表头是左对齐、数据是右对齐**。
  改为显式写 `.table th.num` 把权重提到 0-2-1
- 仓库页、记录页的**每列之间加分割线**（`border-right`，末列不加，避免与卡片边框叠成双线）
- 记录页表头与数据**一一对应**：原第 6 列「操作」压在 出库/入库 标签上、
  真正的按钮列没有表头。改为「类型」对标签、「操作」对按钮
- 记录页新增**时间起止筛选**（含当日，两端闭区间）：
  按 `time` 的前 10 位日期字符串比较，截止日当天 23:59 的记录也会保留；
  两端可只填其一；起始晚于截止时给出明确提示
- 导出 Excel 改为**导出当前筛选结果**（界面看到几条就导出几条），
  按钮在筛选时标明条数；筛选时标题显示「筛选出 N / 共 M 条」

### 端到端自检（真实 Electron + IPC + 磁盘）

- 新增 `scripts/verify-electron.ts` 与 `npm run verify:electron`：用**隐藏窗口**跑真实
  Electron，覆盖此前完全没被验证过的一段链路 ——
  `渲染进程 → contextBridge → IPC → 主进程 → 磁盘 → 再读回渲染进程`
  （浏览器预览版只有界面 + mock 后端，verify:store 只有数据层，两者都到不了 IPC）
- 共 58 项断言，13 个分组：冷启动、桥接往返、提交落盘、单位锁定、负库存警告、
  变更广播与取消订阅、界面自动刷新（读具体单元格而非模糊文本）、撤销反向冲销、
  导出 Excel 的主进程写盘与取消分支、模拟重启后从磁盘读回、损坏恢复的真实启动路径、
  主文件与备份都坏时明确报错、渲染进程控制台零错误
- 顺带把「损坏恢复」两个缺陷锁进回归：恢复成功不抛异常、好备份不被坏文件覆盖，
  并验证界面确实弹出了恢复提示条
- 数据全部写在临时 userData 目录，不触碰真实应用数据

### CI 模板与性能实测

- 新增 `ci/github-actions-build.yml`：GitHub Actions 工作流待安装模板
  （放在 `ci/` 而非 `.github/workflows/`，因为推送后者需要 token 具备 `workflow` scope；
  文件头部写明了两种安装方式）
- 工作流内容：类型检查 + 数据层自检通过后，推 tag 自动打 macOS dmg 与 Windows exe
- 预览工具新增 `?seed=N` 参数，可灌入 N 条记录用于观察数据量增长下的界面表现
- 实测记录页性能：1 万条约 780ms、10 万 DOM 节点、33MB 堆内存，线性增长，
  在可预见的使用规模内无需优化

### 数据文件可见性 + 修复损坏恢复的两个缺陷

- 新增「打开数据文件夹」按钮（界面右上角），一键在访达 / 资源管理器里定位数据目录，
  鼠标悬停可看到数据文件完整路径
- 新增启动提示条：本次启动若发生过「从备份恢复」，界面明确提示可能丢失最后一次操作
- **修复：损坏恢复成功后不再抛异常**。原实现恢复成功却 `throw` 一个标记错误，
  而启动流程里是裸的 `await load()`，异常无人接收 → 数据文件一坏应用直接起不来，
  哪怕数据其实已经救回来了。改为通过 `getLoadReport()` 查询状态
- **修复：恢复时不再毁掉好备份**。原实现调用 `commit(bakData)` 少了 `skipBackup`，
  会先把损坏的主文件拷成 `.bak`，把唯一的好备份覆盖掉
- 主文件与备份都不可用时，改为弹出错误对话框说明原因与路径后退出，
  不再默默当成空库（那样用户一操作就会把仅存的数据覆盖掉）
- 数据层自检从 41 项扩充到 45 项，新增断言覆盖上述三个行为

### 浏览器预览工具

- 新增 `preview/mock.ts`：内存版数据后端，复刻主进程 store 语义（名称归一化、单位锁定、负库存警告、撤销反向冲销）
- 新增 `preview/build-preview.mjs`：注入内存后端并起本地静态服务，只生成 `index.html` + `mock-api.js` 两个文件，资源相对路径指回 `out/renderer`，不复制构建产物
- 新增 `预览界面.command`：双击即可在浏览器里看界面（数据不落盘，刷新即重置）
- 用途：不启动 Electron 也能验证界面与交互

### 应用图标与界面修正

- 新增应用图标（等距纸箱）：`build/icon.png`（1024×1024）+ `build/icon.svg` 源文件
- 打包验证通过：`.app` 内的 `icon.icns` 已替换为自定义图标
- 修正表单输入框宽度不一致：名称/操作人因外层定位容器未撑满，比数量/单位窄
- 修正操作人自动补全：已输入完整姓名时不再重复推荐自身

### 打包配置（P6）

- `electron-builder.yml` 新增 macOS dmg 打包配置（arm64 + x64）
- `package.json` 新增 `build:mac` 脚本
- macOS arm64 打包已验证成功，产物位于 `dist/mac-arm64/库存管理系统.app`
- GitHub Actions 工作流文件因 OAuth scope 限制无法推送，需手动在仓库设置中添加

### 记录页增强（P4）

- 记录页新增搜索框（按名称模糊匹配）和出入库类型筛选（全部/入库/出库）
- 每行记录新增「撤销」按钮，确认后调用 `deleteRecord` 反向冲销库存
- 新增「导出 Excel」按钮：渲染进程用 SheetJS 0.20.3 生成 xlsx buffer，主进程弹 dialog 保存到用户指定路径
- IPC 新增 `db:deleteRecord`（已有）和 `export:xlsx` 通道

### 仓库页、记录页、操作页（P3）

- 仓库页物品列表新增「本月入库」「本月出库」两列，按自然月统计（表头显示当前月份）
- 仓库页每行新增「入库」「出库」按钮，点击弹出表单弹窗，预填物品名称、单位自动锁定
- 记录页新增「操作人」列，未填时显示 `—`
- 操作页改为双栏布局（左入库、右出库），共用同一套 TransactionForm 组件
- 表单字段：时间（默认系统当前时间）、名称（自动补全提示）、数量、单位（已存在物品时自动锁定）、操作人（自动补全提示，选填，记住上次输入）
- 新增损坏恢复：启动时若 `data.json` 读不了或解析失败，自动从 `.bak` 恢复，恢复成功后立刻重写主文件防止好备份被覆盖
- 旧数据兼容：加载时自动给没有 `operator` 字段的旧记录补空串
- 数据层自检扩充到 41 项，全部通过（含损坏恢复、旧数据迁移）
- 新增双击启动脚本 `启动开发环境.command`（无需全局安装 Node，自动使用 WorkBuddy 内置环境）

### 数据层（P2）

- 实现数据存储：单个 JSON 文件，原子写入（临时文件 + fsync + rename 覆盖），保留 `.bak` 备份
- 实现串行写队列，并发提交不会互相覆盖
- 实现统一写入口 `applyTransaction()`：**写记录与改库存原子完成**，任一步失败都不落盘，杜绝「记录有了、库存没减」
- 单位锁定下沉到数据层：物品已存在时强制忽略调用方传入的单位，绕过界面也改不掉
- 撤销记录支持**反向冲销库存**（删入库记录减库存、删出库记录加库存）
- 出库超库存只警告不阻断，库存允许为负
- 数量统一保留 3 位小数，避免浮点累加产生 `0.30000000000000004`
- 新增数据层自检脚本（`npm run verify:store`），37 项断言全部通过：含并发写、浮点精度、持久化重载、失败不污染内存
- 仓库页与记录页接通真实数据，主进程变更后自动广播，所有页面同步刷新

### 工程骨架（P1）

- 初始化项目：建立 Electron + React + TypeScript 工程骨架
- 锁定依赖版本：electron 44 / vite 7 / electron-vite 5 / electron-builder 26
  （注意：vite 8 与 electron-vite 5 不兼容，必须锁 `^7`；`@vitejs/plugin-react` 需用 5.x）
- 配置 Electron 二进制下载镜像，规避本机代理对 GitHub CDN 的 502 问题
- 主进程：窗口创建 + 单实例锁（防多开双写同一数据文件）
